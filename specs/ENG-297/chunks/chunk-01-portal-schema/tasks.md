# Chunk: chunk-01-portal-schema

- [x] T-003: Run GitNexus analyze plus impact checks for the shared schema and migration symbols before editing them
- [x] T-010: Update `convex/schema.ts` with the portal registry, `users.homePortalId`, placeholder portal attachment tables, and required indexes/comments
- [x] T-011: Create `convex/portals/validators.ts` and `convex/portals/queries.ts`
- [x] T-012: Extend `convex/brokers/migrations.ts` with FairLend seed and `homePortalId` backfill/status runners
