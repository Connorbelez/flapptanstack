# Chunk Context: chunk-02-portal-route-shell

## Goal
- Add the `/portal` route shell protected by route auth and Convex auth readiness gates.

## Relevant plan excerpts
- `src/routes/portal.tsx` should use `beforeLoad: guardRouteAccess("micPortal")`.
- Layout pattern:
  - `<Authenticated><OutletOrContent /></Authenticated>`
  - `<AuthLoading><AppRoutePendingScreen /></AuthLoading>`
- The route may render a protected placeholder shell until ENG-357 wires dashboard UI.

## Implementation notes
- Follow `src/routes/listings/route.tsx` for `Authenticated` / `AuthLoading`.
- Follow `src/routes/lender.portfolio.tsx` for active portal assertion only if the route content needs a portal id.
- Do not add suspense queries or dashboard data consumers in this issue.

## Existing code touchpoints
- New `src/routes/portal.tsx`.
- `src/routeTree.gen.ts` may be regenerated by the route generation/typecheck flow.
- GitNexus impact:
  - `ListingsLayout`: LOW, no indexed upstream dependents.

## Validation
- Route/component tests for shell gating.
- `bun typecheck` to verify file-route generation and route typing.
