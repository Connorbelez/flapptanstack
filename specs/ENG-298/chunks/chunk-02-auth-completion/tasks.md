# Chunk: chunk-02-auth-completion

- [x] T-020: Add the post-auth completion surface and supporting data lookup for `homePortalId` resolution and fail-closed validation.
- [x] T-021: Add same-host wrong-portal rejection UI with a continue CTA and explicit admin bypass logic.
- [x] T-022: Keep the callback round trip on supported AuthKit APIs and wire the completion flow through same-origin `returnPathname`.
